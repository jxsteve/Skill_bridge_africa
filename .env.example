# Copy to .env and fill in. Values are read at build time by Expo.

# Privy app credentials (dashboard.privy.io -> your app -> Settings)
EXPO_PUBLIC_PRIVY_APP_ID=cmqy53hyi00mm0clerka9busw
# Mobile client ID, found under Settings -> Clients (starts with "client-")
EXPO_PUBLIC_PRIVY_CLIENT_ID=
